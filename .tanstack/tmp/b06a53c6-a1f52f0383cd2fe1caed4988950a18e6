import { createFileRoute, Outlet } from "@tanstack/react-router"

export const Route = createFileRoute("/$orgSlug/team/$teamSlug")({
  component: TeamLayout,
})

function TeamLayout() {
  return <Outlet />
}
